<div class="flex h-screen">
  {% if article %}
    <div class="flex-1 overflow-y-auto bg-orange-400">
      {% if article.image %}
        <div class="bg-black text-red-500">
          <img class="w-full h-300" src="{{ article.image.src | img_url: 'master' }}" alt="{{ article.title }}" />
          <p class="p-5">read more</p>
        </div>
      {% endif %}
    </div>
    <div class="h-screen w-1/3 md:w-2/3 bg-orange-400 flex justify-center overflow-hidden">
      k
    </div>
  {% endif %}
</div>